const path = require('path');

module.exports = {
    path: path.resolve(process.cwd(), '.'),
    interval: 1000, // ms
    subscribe: { // subscribe command options

    },
    port: 12355,
    host: '127.0.0.1'
};
